# KYAKUMAP テスト手順

## 固定デモ

1. `npm run db:seed && npm run db:verify`
2. `/customers`を開く
3. 佐藤健一カードに「新しくつながった情報」が表示されることを確認
4. 顧客マップを開き「マップで見る」を押す
5. キーパーソン領域の情報がアンロックされ、山本隆との関係が表示されることを確認
6. 閉じる操作、キーボード操作、375px表示を確認

## API課金

知識抽出の実測は、既存の `.logs/turns.jsonl` を先に確認する。同じ原文を繰り返し送らない。必要な場合も固定合成文1件だけで行う。

無料モデルの接続確認は`npm run probe:free`を使う。catalog上の無料価格を確認してから最小リクエストを1回だけ送り、失敗理由を区別する。有料モデルへ自動で切り替えない。

## 根拠付き顧客AI

課金なしの確認:

1. `npm run db:seed && npm run db:verify`
2. 佐藤健一の詳細を開く
3. 音声ボタンがキーボード操作でき、テキスト欄にもラベルがあることを確認
4. APIレスポンスの人物リンクが`/customers/{DB上のentityId}`からのみ生成されることをコードで確認
5. `npm run check`

実APIの固定確認（必要時に各1回だけ）:

- 既知: 「趣味は何ですか？」→ 海釣りを「俺」の一人称で回答し、訪問記録の根拠を返す
- 未知: 「お父さんの会社はどんな会社？」→ 情報不足を明示し、DB上の関係者候補カードを返す
- 候補カードから顧客詳細へ遷移できる

2026-09-21確認結果（`deepseek/deepseek-v4-flash-free`）:

- `npm run probe:free`: JSON応答成功
- 既知質問: HTTP 200、`known`、海釣り、根拠1件
- 未知質問: HTTP 200、`unknown`、根拠0件、山本隆を情報保持者候補として返却
- 解決モデル: `deepseek-v4-flash-ga-260731`
- TTSは無料モデルを使用せず、`ORCAROUTER_TTS_MODEL=browser`でWeb Speech APIへフォールバック

実行前に`npm run logs`を読み、同じケースが記録済みなら再実行しない。TTSも回答ごとに課金されるため総当たりしない。
